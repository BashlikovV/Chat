<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        package="by.bashlikovv.messenger">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
            android:name=".presentation.App"
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:networkSecurityConfig="@xml/network_security_config"
            android:supportsRtl="true"
            android:theme="@style/Theme.Chat"
            android:usesCleartextTraffic="true"
            tools:targetApi="34">
        <profileable android:shell="true" />
        <activity
                android:name="by.bashlikovv.messenger.presentation.view.LogInActivity"
                android:exported="true"
                android:theme="@style/Theme.Chat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
                android:name="by.bashlikovv.messenger.presentation.view.ChatActivity"
                android:exported="false"
                android:label="@string/title_activity_chat"
                android:theme="@style/Theme.Chat" />
        <activity
                android:name="by.bashlikovv.messenger.presentation.view.MessengerActivity"
                android:exported="false"
                android:theme="@style/Theme.Chat" />
    </application>

</manifest>